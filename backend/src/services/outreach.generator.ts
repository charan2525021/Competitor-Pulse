// Legacy file — no longer used in CompetitorPulse mode
// Kept for backward compatibility

export async function generateMessage(
  _company: string,
  _name: string | null,
  _role: string | null,
  _context?: string
): Promise<string> {
  return "";
}
